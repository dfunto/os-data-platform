name: example_source
description: "Example S3 source for testing"
source_type: "s3"
s3_config:
  bucket: example-bucket
  disable_auth: true
  tables:
    - name: example_table
      prefix: "example/data/"
      file_format: Parquet
      full_refresh: true