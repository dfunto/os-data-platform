[project]
name = "orchestrator"
version = "0.1.0"
description = "Data Platform orchestrator"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dagster",
    "dagster-postgres",
    "dagster-k8s",
    "dagster-webserver",
]