[project]
name = "transform"
version = "0.1.0"
description = "Data platform transformation pipelines (dbt / ClickHouse)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dbt-core==1.9.4",
    "dbt-clickhouse==1.9.2",
]
